using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class script_arbol : MonoBehaviour {
    // Start is called before the first frame update
    void Start() {
        Debug.Log("4. Objeto árbol");
    }

    // Update is called once per frame
    void Update() {
        int h = h + 1;
        Debug.Log("Contador árbol= " + h);
    }
}
